// import backend from "../services/backend";
// import { useQuery } from "react-query";

// export const useAnimalQuery = () => {
//   const list = useQuery({
//     queryKey: ["animals"],
//     queryFn: () => backend.animalService.list().then((res) => res),
//   });

//   //   const find = useQuery({
//   //     queryKey: ["animal", id],
//   //     queryFn: () => backend.animalService.find(id).then((res) => res.data),
//   //   });

//   return { list };
// };
